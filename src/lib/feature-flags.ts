/**
 * Master switch for Donate (Razorpay / Stripe) on project pages and payment session APIs.
 * Implementation stays in place; flip to true when donations should be live again.
 */
export const DONATIONS_ENABLED = false
